# FFmpeg Tools

Place `ffmpeg.exe` and `ffprobe.exe` in this folder for bundled recording and probing.

The source review package omits these binaries and the downloaded ZIP archives to keep the archive small. The project file copies the tools only when the files exist.
